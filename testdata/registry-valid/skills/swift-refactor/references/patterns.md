# Swift Refactoring Patterns

Common patterns for safe Swift refactoring.

## Extract Method

Pull repeated logic into a named function with clear parameters.

## Replace Conditional with Polymorphism

Use protocol conformance instead of switch/if chains.
